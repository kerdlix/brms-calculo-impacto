package com.redhat.impacto;

import java.lang.Number;

rule "SomaRecebimentos"
	dialect "mvel"
	when
		$c : Calculo( )
		$total : Number( ) from accumulate ( 		(Recebimento( $r : recebimento) from $c.recebimentos)
,
			sum($r)) 
	then
end
